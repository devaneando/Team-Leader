# Learn more about services, parameters and containers at
# https://symfony.com/doc/current/service_container.html
parameters:
    app.repositoy.category.class: AppBundle\Entity\Repository\CategoryRepository
    app.repositoy.product.class: AppBundle\Entity\Repository\ProductRepository
    app.command.manage_category.class: AppBundle\Command\ManageCategoryCommand
    app.command.manage_product.class: AppBundle\Command\ManageProductCommand

services:
    _defaults:
        autowire: true
        # Normally I prefer to have control over the services, so I disabled autoconfigure.
        autoconfigure: false
        public: false

    ##### Doctrine entity repositories

    app.repositoy.category:
        class: '%app.repositoy.category.class%'
        public: true
        factory: ['@doctrine.orm.default_entity_manager', getRepository]
        arguments: [ AppBundle\Entity\Category ]
        tags:
            - { name: 'doctrine.repository_service' }

    app.repositoy.product:
        class: '%app.repositoy.product.class%'
        public: true
        factory: ['@doctrine.orm.default_entity_manager', getRepository]
        arguments: [ AppBundle\Entity\Product ]
        tags:
            - { name: 'doctrine.repository_service' }

    ##### Commands

    app.command.manage_category:
        class: '%app.command.manage_category.class%'
        public: true
        calls:
            - [setCategoryRepository, ['@app.repositoy.category']]
            - [setEntityManager, ['@doctrine.orm.entity_manager']]
        tags:
            - { name: 'console.command' }

    app.command.manage_product:
        class: '%app.command.manage_product.class%'
        public: true
        calls:
            - [setCategoryRepository, ['@app.repositoy.category']]
            - [setProductRepository, ['@app.repositoy.product']]
            - [setEntityManager, ['@doctrine.orm.entity_manager']]
        tags:
            - { name: 'console.command' }
